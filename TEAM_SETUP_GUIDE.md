# 사이트 변경 모니터링 툴 — 팀원용 설치 가이드

이 문서 하나만 따라 하면, 본인 PC에서 이 툴을 처음부터 세팅할 수 있습니다.
개발 지식이 없어도 됩니다. 순서대로만 진행하세요.

---

## 1. 이 툴이 하는 일

매일 정해진 시간에 자동으로:

1. 지정한 사이트들(PC/모바일)에 접속해 **스크린샷**을 찍고
2. **전일 캡쳐와 비교**해서 변경이 있는지 판단하고
3. **HTML 리포트**를 만들어
4. **구글 드라이브**에 올리고, **웹(GitHub Pages)** 으로도 공개하고
5. **두레이 메신저**로 결과를 알립니다.

사람이 PC 앞에 없어도 매일 자동으로 돕니다(PC는 켜져 있어야 함).

---

## 2. 준비물 (최초 1회 설치)

아래 3개를 순서대로 설치하세요. 전부 기본값(Next만 계속)으로 설치하면 됩니다.

1. **Node.js** — https://nodejs.org 에서 초록색 **LTS** 버전
2. **Git** — https://git-scm.com/download/win (웹 리포트 업로드에 필요. GitHub 로그인 관리자가 같이 깔립니다)
3. **크롬 브라우저** — 이미 있으면 생략

설치 확인: 키보드 `Win + R` → `cmd` → 엔터 → 검은 창에 `node -v` 입력.
`v20.x.x` 같은 버전이 나오면 성공.

---

## 3. 프로그램 파일 받기

두 가지 방법 중 하나로 프로그램 폴더를 준비합니다.

**방법 A — GitHub에서 내려받기 (권장)**
1. 담당자에게 GitHub 저장소 주소를 받습니다 (예: `github.com/OOO/site-monitor`)
2. 그 페이지에서 초록색 **Code → Download ZIP**
3. 압축을 풀어 **`C:\site-monitor`** 폴더로 옮깁니다 (경로에 한글/공백 없는 걸 권장)

**방법 B — 담당자에게 폴더 직접 받기**
`node_modules`, `screenshots`, `diffs`, `results`, `reports` 폴더는 빼고,
코드 파일들만 받아 `C:\site-monitor`에 두면 됩니다.

폴더 안에 최소 이 파일들이 있어야 합니다:
`monitor.js, compare.js, report.js, notify.js, copy.js, publish.js, package.json, urls.csv, setup.bat, run.bat`

---

## 4. 최초 설치 — setup.bat

`C:\site-monitor` 폴더에서 **`setup.bat` 더블클릭**.
검은 창에서 필요한 프로그램(브라우저 엔진, 라이브러리 등)이 자동으로 설치됩니다 (몇 분 소요).
"Setup complete!"가 뜨면 아무 키나 눌러 닫습니다.

> "Windows의 PC 보호" 경고가 뜨면 **추가 정보 → 실행**을 누르세요.

---

## 5. 감시할 사이트 관리 — urls.csv

`urls.csv`를 **메모장**으로 열어 감시할 사이트를 적습니다.
첫 줄(제목)은 그대로 두고, 한 줄에 사이트 하나씩 적습니다.

```
사이트명,URL,중요도,확인영역,무시영역,다음버튼,탭버튼,모바일
홈 상시 387,https://www.lguplus.com/benefit-event/ongoing/387,상,,,,,
홈 상시 387 (모바일),https://m.lguplus.com/benefit-event/ongoing/387,상,,,,,Y
```

| 칸 | 의미 | 필수? |
|---|---|---|
| 사이트명 | 리포트에 표시될 이름 | 필수 |
| URL | http(s) 전체 주소 (모바일은 `m.` 주소 사용) | 필수 |
| 중요도 | 상/중/하 ("상"은 변경 시 🚨 강조) | 비우면 "중" |
| 확인영역 | 이 부분만 비교할 CSS 셀렉터 | 비워도 됨 |
| 무시영역 | 광고/롤링배너 등 비교에서 뺄 CSS 셀렉터 | 비워도 됨 |
| 다음버튼 | (고급) 슬라이드 화살표 셀렉터 | 비워도 됨 |
| 탭버튼 | (고급) 탭 셀렉터, 여러 개는 `\|`로 구분 | 비워도 됨 |
| **모바일** | **`Y`면 모바일(아이폰) 화면으로 캡쳐** | 비우면 PC |

> **중요 (모바일):** 모바일 행은 URL을 반드시 **`m.lguplus.com`** 으로 넣으세요.
> `www` 주소는 모바일로 접속해도 데스크톱 화면만 나옵니다.

> **저장 시 주의:** 메모장에서 저장할 때 인코딩이 **UTF-8**이어야 한글이 안 깨집니다
> (Windows 10/11 메모장 기본값이 UTF-8이라 대개 괜찮습니다).

---

## 6. 설정 파일 4개 (개인/팀별로 직접 입력)

아래 4개 파일은 보안·개인정보라서 배포에 포함되지 않습니다.
메모장으로 **새로 만들어**, 각 파일에 **한 줄만** 넣고 저장하세요.
(필요 없는 기능은 해당 파일을 안 만들면 그 단계는 자동으로 건너뜁니다.)

| 파일 | 넣을 내용 | 용도 |
|---|---|---|
| `webhook.txt` | 두레이 웹훅 주소 (`https://hook.dooray.com/services/...`) | 메신저 알림 |
| `share.txt` | 구글 드라이브 폴더의 **내 PC 경로** (예: `G:\내 드라이브\사이트 모니터링`) | 파일 업로드 |
| `link.txt` | 그 구글 드라이브 폴더의 **공유 링크** | 메시지에 표시 |
| `weblink.txt` | GitHub Pages 주소 (예: `https://OOO.github.io/site-monitor/`) | 메시지에 표시 |

### 6-1. 두레이 웹훅 (webhook.txt)
1. 알림 받을 **두레이 채팅방**(팀 방) 열기
2. 채팅방 설정 → **서비스 연동 / 커넥트** → **Incoming Webhook(들어오는 웹훅)** 추가
3. 생성된 URL을 복사해 `webhook.txt`에 붙여넣기
   - 권한이 없으면 방 관리자에게 요청하세요.

### 6-2. 구글 드라이브 (share.txt, link.txt)
1. **Google Drive for Desktop** 설치·로그인 (drive.google.com/download)
2. 내 드라이브(또는 공유 드라이브)에 폴더를 하나 만들기
3. 파일 탐색기에서 그 폴더를 열고 **주소창을 클릭** → 전체 경로 복사 → `share.txt`에 붙여넣기
4. drive.google.com에서 그 폴더 우클릭 → **공유** → "링크가 있는 모든 사용자"(또는 팀) → **링크 복사** → `link.txt`에 붙여넣기

---

## 7. 테스트 실행 — run.bat

`run.bat` 더블클릭. 검은 창에서 6단계가 순서대로 돌고, 끝나면 리포트가 브라우저로 열립니다.

- 첫 실행은 비교할 "어제"가 없어 전부 **"첫 실행"**으로 표시됩니다. 다음 날부터 비교가 시작됩니다.
- `screenshots\오늘날짜` 폴더에 이미지가 제대로 찍혔는지 확인하세요.

---

## 8. 웹 리포트 공개 — GitHub Pages (선택, 팀 공유에 유용)

링크 하나로 팀원 누구나 이미지까지 웹에서 바로 볼 수 있게 합니다.

> **여러 명이 각자 실행한다면**, 웹 리포트는 **각자 본인 GitHub 계정/저장소**를 쓰세요.
> 한 저장소를 여러 PC가 동시에 올리면 충돌합니다. (되도록 **실행은 한 대(주 담당 PC)** 로 정하는 걸 권장)

1. github.com 에서 **New repository** → 이름 `site-monitor` → **Public** → Create
2. `web-setup.bat` 안의 저장소 주소를 본인 것으로 바꿉니다
   - 메모장으로 `web-setup.bat` 열기 → `https://github.com/OOO/site-monitor.git` 부분을 본인 주소로 수정
   - 같은 방식으로 `run.bat` 안에는 주소가 없으니 수정 불필요, `weblink.txt`만 본인 주소로
3. `web-setup.bat` 실행 → GitHub 로그인 창이 뜨면 **끝까지 로그인** → 자동 업로드
   - 하단에 `main -> main` 이 보이면 성공. `error/fatal`이 보이면 담당자에게 문의.
4. GitHub 저장소 → **Settings → Pages** → Source: *Deploy from a branch*, Branch **main**, 폴더 **/docs** → Save
5. 1~2분 뒤 `https://본인아이디.github.io/site-monitor/` 접속 → 리포트가 보이면 완료
6. 그 주소를 `weblink.txt`에 넣기 (두레이 메시지에 링크로 표시됩니다)

> 웹 리포트가 안 올라가면 **web-fix.bat**을 실행하세요(오류 메시지가 화면에 표시됩니다).

---

## 9. 매일 자동 실행 — 작업 스케줄러

1. `Win` 키 → **"작업 스케줄러"** 검색해 실행
2. 오른쪽 **기본 작업 만들기**
3. 이름: `사이트 모니터링` → 다음
4. 트리거: **매일** → 다음 → 시작 시간 **오전 9:00** → 다음
5. 동작: **프로그램 시작** → 다음 → **찾아보기**로 `C:\site-monitor\run.bat` 선택 → 다음 → 마침
6. (권장) 만든 작업 더블클릭 → **설정** 탭 → **"예약된 시작을 놓친 경우 가능한 대로 빨리 작업 시작"** 체크 → 확인
   - 9시에 PC가 꺼져 있었어도, 켜면 곧 실행됩니다.

이제 매일 아침 자동으로 점검·리포트·업로드·알림까지 진행됩니다.

---

## 10. 운영 팁

- **사이트 추가/삭제**: `urls.csv`를 메모장으로 수정 후 저장. 다음 실행부터 반영됩니다.
- **광고/롤링배너가 매일 "변경"으로 뜰 때**: 그 영역의 CSS 셀렉터를 **무시영역** 칸에 넣으세요.
  - 셀렉터 찾는 법: 크롬에서 해당 영역 우클릭 → **검사** → 파란 줄 우클릭 → **Copy → Copy selector**.
  - 롤링 배너는 이미 "1번 슬라이드 고정" 처리가 되어 있어 대부분 문제없습니다.
- **오래된 스크린샷**: 14일 지나면 자동 삭제됩니다(용량 관리).
- **두레이 방 변경**: `webhook.txt`의 주소만 새 방 웹훅으로 바꾸면 됩니다.

---

## 11. 문제 해결 (FAQ)

- **`run.bat` 창이 바로 꺼져요** → `Win+R` → `cmd` → `cd C:\site-monitor` 엔터 → `run.bat` 엔터 → 나오는 오류 확인.
- **한글이 깨져요(사이트명)** → `urls.csv`를 메모장 "다른 이름으로 저장"에서 인코딩을 **UTF-8**로 다시 저장.
- **모바일이 데스크톱 화면으로 나와요** → 모바일 행 URL이 `m.lguplus.com`인지 확인.
- **웹 리포트가 404** → `web-fix.bat` 실행 후 화면의 결과 확인. GitHub Pages 설정(main /docs)도 재확인.
- **구글 드라이브에 안 올라가요** → 구글 드라이브 데스크톱이 실행 중인지, `share.txt` 경로가 정확한지 확인.
- **두레이 알림이 안 와요** → `webhook.txt`의 주소가 올바른지, 그 방에 웹훅이 활성화됐는지 확인.

---

## 12. 파일 설명 (참고용, 건드릴 필요 없음)

| 파일 | 역할 |
|---|---|
| `monitor.js` | 1단계 — 사이트 접속 & 스크린샷 |
| `compare.js` | 2단계 — 전일/금일 비교 (밀림 보정 포함) |
| `report.js` | 3단계 — 로컬 HTML 리포트 생성 |
| `copy.js` | 4단계 — 구글 드라이브 폴더로 복사 |
| `publish.js` | 5단계 — 웹용 리포트(docs) 생성 |
| `notify.js` | 6단계 — 두레이 메신저 알림 |
| `run.bat` | 위 6단계를 순서대로 실행 (매일 자동 실행 대상) |
| `setup.bat` | 최초 1회 설치 |
| `web-setup.bat` / `web-fix.bat` | GitHub 최초 연결 / 업로드 문제 해결 |
| `urls.csv` | 감시할 사이트 목록 |
| `webhook/share/link/weblink.txt` | 개인·팀별 설정값 |
| `test-*.bat` | 특정 페이지 캡쳐를 빠르게 점검하는 테스트(선택) |

---

궁금한 점이나 오류가 있으면, 검은 창(cmd)에 나온 메시지를 캡쳐해 담당자에게 문의하세요.
