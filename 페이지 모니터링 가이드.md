# 사이트 변경 모니터링 툴 — 설치 가이드 (최종본)

이 문서 하나만 순서대로 따라 하면, 본인 PC에서 이 툴을 처음부터 세팅할 수 있습니다.
개발 지식이 없어도 됩니다.

---

## 1. 이 툴이 하는 일

매일 정해진 시간에 자동으로:

1. 지정한 사이트들(PC/모바일)에 접속해 **스크린샷**을 찍고
2. **전일(가장 최근 실행분)과 비교**해 변경이 있는지 판단하고
3. **HTML 리포트**를 만들어
4. **구글 드라이브**에 올리고, **웹(GitHub Pages)** 으로도 공개하고
5. **두레이 메신저**로 결과를 알립니다.

똑똑한 처리들이 들어가 있습니다(신경 안 써도 자동으로 됨):

- **롤링 배너 자동 고정**: 자동으로 넘어가는 배너(Swiper/Slick)를 항상 1번 슬라이드로 고정해 오탐 방지
- **실제 모바일 화면**: 모바일 행은 진짜 폰 화면으로 캡쳐
- **지연 로딩 이미지 강제 로드**: 하단이 비는 문제 방지
- **밀림 보정 + 움직임 영역 자동 제외**: 몇 픽셀 밀리거나 광고가 도는 정도는 무시
- **하단 고정 배너 정리**: 본문을 가리지 않게 맨 아래로

사람이 PC 앞에 없어도 매일 자동으로 돕니다(단, 실행 시각에 PC가 켜져 있어야 함).

---

## 2. 준비물 (최초 1회 설치)

아래 3개를 순서대로 설치하세요. 전부 기본값(Next만 계속)으로 설치하면 됩니다.

1. **Node.js** — https://nodejs.org 에서 초록색 **LTS** 버전
2. **Git** — https://git-scm.com/download/win (웹 리포트 업로드용. GitHub 로그인 관리자가 같이 깔립니다)
3. **크롬 브라우저** — 이미 있으면 생략

설치 확인: `Win + R` → `cmd` → 엔터 → 검은 창에 `node -v` → `v20.x.x` 처럼 나오면 성공.

---

## 3. 프로그램 파일 받기

**GitHub에서 내려받기 (권장)**

1. 담당자에게 GitHub 저장소 주소를 받습니다 (예: `github.com/OOO/site-monitor`)
2. 그 페이지에서 초록색 **Code → Download ZIP**
3. 압축을 풀어 **`C:\site-monitor`** 폴더로 옮깁니다 (경로에 한글/공백 없는 걸 권장)

폴더 안에 최소 이 파일들이 있어야 합니다:
`monitor.js, compare.js, report.js, notify.js, copy.js, publish.js, package.json, urls.csv, setup.bat, run.bat, web-setup.bat`

---

## 4. 최초 설치 — setup.bat

`C:\site-monitor` 폴더에서 **`setup.bat` 더블클릭**.
필요한 프로그램(브라우저 엔진, 라이브러리)이 자동 설치됩니다 (몇 분 소요).
"Setup complete!"가 뜨면 아무 키나 눌러 닫습니다.

> "Windows의 PC 보호" 경고가 뜨면 **추가 정보 → 실행**을 누르세요.

---

## 5. 감시할 사이트 관리 — urls.csv

`urls.csv`를 **메모장**으로 열어 감시할 사이트를 적습니다.
첫 줄(제목)은 그대로 두고, 한 줄에 사이트 하나씩.

```
사이트명,URL,중요도,확인영역,무시영역,다음버튼,탭버튼,모바일
소호 메인,https://www.lguplus.com/internet-iptv/soho,상,,,,,
소호 메인 (모바일),https://m.lguplus.com/internet-iptv/soho,상,,,,,Y
```

| 칸 | 의미 | 필수? |
|---|---|---|
| 사이트명 | 리포트에 표시될 이름 | 필수 |
| URL | http(s) 전체 주소 (모바일은 `m.` 주소) | 필수 |
| 중요도 | 상/중/하 ("상"은 변경 시 🚨 강조) | 비우면 "중" |
| 확인영역 | 이 부분만 비교할 CSS 셀렉터 | 비워도 됨 |
| 무시영역 | 특정 영역을 비교에서 뺄 CSS 셀렉터 | 비워도 됨 |
| 다음버튼 / 탭버튼 | (거의 안 씀) 고급 옵션 | 비워도 됨 |
| **모바일** | **`Y`면 모바일(폰) 화면으로 캡쳐** | 비우면 PC |

> **모바일은 URL을 반드시 `m.lguplus.com` 으로!** `www`는 모바일로 접속해도 데스크톱만 나옵니다.
> **저장 시 인코딩은 UTF-8** (Windows 10/11 메모장 기본값이라 대개 괜찮음).

---

## 6. 설정 파일 4개 (개인/팀별로 직접 입력)

아래 4개는 보안·개인정보라서 배포에 포함되지 않습니다.
메모장으로 **새로 만들어**, 각 파일에 **한 줄만** 넣고 저장하세요.
(필요 없는 기능은 파일을 안 만들면 그 단계는 자동으로 건너뜁니다.)

| 파일 | 넣을 내용 | 용도 |
|---|---|---|
| `webhook.txt` | 두레이 웹훅 주소 (`https://hook.dooray.com/services/...`) | 메신저 알림 |
| `share.txt` | 구글 드라이브 폴더의 **내 PC 경로** (예: `G:\내 드라이브\사이트 모니터링`) | 파일 업로드 |
| `link.txt` | 그 구글 드라이브 폴더의 **공유 링크** | 메시지에 표시 |
| `weblink.txt` | GitHub Pages 주소 (예: `https://OOO.github.io/site-monitor/`) | 메시지에 표시 |

### 6-1. 두레이 웹훅 (webhook.txt)
1. 알림 받을 **두레이 채팅방**(팀 방) 열기
2. 채팅방 설정 → **서비스 연동 / 커넥트** → **Incoming Webhook** 추가
3. 생성된 URL을 `webhook.txt`에 붙여넣기 (권한 없으면 방 관리자에게 요청)

### 6-2. 구글 드라이브 (share.txt, link.txt)
1. **Google Drive for Desktop** 설치·로그인 (drive.google.com/download)
2. 드라이브에 폴더를 하나 만들기
3. 파일 탐색기에서 그 폴더를 열고 주소창 클릭 → 전체 경로 복사 → `share.txt`
4. drive.google.com에서 그 폴더 우클릭 → **공유** → "링크가 있는 모든 사용자"(또는 팀) → 링크 복사 → `link.txt`

---

## 7. 테스트 실행 — run.bat

`run.bat` 더블클릭. 검은 창에서 단계별로 돌고, 끝나면 리포트가 브라우저로 열립니다.

- 첫 실행은 비교할 "어제"가 없어 전부 **"첫 실행"**으로 표시됩니다. 다음 날부터 비교 시작.
- `screenshots\오늘날짜` 폴더에 이미지가 제대로 찍혔는지 확인하세요.
- 실행 시간: 사이트 수 × 몇 초 (움직임 감지·이미지 로드 때문에 넉넉히 걸림).

---

## 8. 웹 리포트 공개 — GitHub Pages

링크 하나로 팀원 누구나 이미지까지 웹에서 바로 볼 수 있게 합니다. **웹 리포트의 이미지는 클릭하면 새 탭에서 크게 볼 수 있습니다.**

> **실행은 되도록 한 대(주 담당 PC)에서만.** 여러 PC가 같은 저장소에 올리면 충돌합니다.
> 각자 실행하려면 각자 본인 GitHub 계정/저장소를 쓰세요.

1. github.com → **New repository** → 이름 `site-monitor` → **Public** → Create
2. 메모장으로 `web-setup.bat` 열기 → 그 안의 `https://github.com/OOO/site-monitor.git` 를 본인 저장소 주소로 수정
3. `web-setup.bat` 실행 → GitHub 로그인 창이 뜨면 **끝까지 로그인** → 자동 업로드
   - 하단에 `main -> main` 이 보이면 성공. `error/fatal`이면 담당자 문의.
4. GitHub 저장소 → **Settings → Pages** → Source: *Deploy from a branch*, Branch **main**, 폴더 **/docs** → Save
5. 1~2분 뒤 `https://본인아이디.github.io/site-monitor/` 접속 → 리포트가 보이면 완료
6. 그 주소를 `weblink.txt`에 넣기 (두레이 메시지에 링크로 표시됨)

> 웹 리포트가 안 올라가거나 404면 **`web-fix.bat`** 실행 (오류 메시지가 화면에 표시됩니다).

---

## 9. 매일 자동 실행 — 작업 스케줄러

1. `Win` 키 → **"작업 스케줄러"** 실행
2. 오른쪽 **기본 작업 만들기**
3. 이름: `사이트 모니터링` → 다음
4. 트리거: **매일** → 다음 → 시작 시간 **오전 9:00** → 다음
5. 동작: **프로그램 시작** → 다음 → **찾아보기**로 `C:\site-monitor\run.bat` 선택 → 다음 → 마침
6. (권장) 만든 작업 더블클릭 → **설정** 탭 → **"예약된 시작을 놓친 경우 가능한 대로 빨리 작업 시작"** 체크 → 확인
   - 9시에 PC가 꺼져 있었어도, 켜면 곧 실행됩니다.

이제 매일 아침 자동으로 점검·리포트·업로드·알림까지 진행됩니다.

---

## 10. 운영 팁

- **사이트 추가/삭제**: `urls.csv`를 메모장으로 수정 후 저장. 다음 실행부터 반영.
- **롤링 배너**: 대부분 자동으로 1번 슬라이드에 고정됩니다(Swiper/Slick 자동 처리). 새 페이지도 대개 자동.
- **그래도 특정 영역이 매일 "변경"으로 뜨면**: 그 영역 CSS 셀렉터를 **무시영역** 칸에 넣으세요.
  - 셀렉터 찾기: 크롬에서 그 영역 우클릭 → **검사** → 파란 줄 우클릭 → **Copy → Copy selector**.
- **오래된 스크린샷**: 14일 지나면 자동 삭제.
- **두레이 방 변경**: `webhook.txt`의 주소만 새 방 웹훅으로 교체.
- **웹 리포트 상세 이미지**: 웹 페이지에서 이미지를 클릭하면 새 탭에서 크게 볼 수 있습니다.

---

## 11. 문제 해결 (FAQ)

- **`run.bat` 창이 바로 꺼져요** → `Win+R` → `cmd` → `cd C:\site-monitor` 엔터 → `run.bat` 엔터 → 나오는 오류 확인.
- **사이트명 한글이 깨져요** → `urls.csv`를 메모장 "다른 이름으로 저장"에서 인코딩 **UTF-8**로 다시 저장.
- **모바일이 데스크톱 화면으로 나와요** → 모바일 행 URL이 `m.lguplus.com`인지 확인.
- **웹 리포트가 404** → `web-fix.bat` 실행 후 결과 확인. Settings→Pages(main /docs)도 재확인.
- **구글 드라이브에 안 올라가요** → 구글 드라이브 데스크톱 실행 중인지, `share.txt` 경로가 정확한지 확인.
- **두레이 알림이 안 와요** → `webhook.txt` 주소가 올바른지, 그 방에 웹훅이 활성화됐는지 확인.
- **캡쳐 방식을 바꾼 첫날엔** 이전과 비교돼 여러 건이 "변경"으로 뜰 수 있습니다. 다음 날부터 정상입니다.

---

## 12. 파일 설명 (참고용, 건드릴 필요 없음)

| 파일 | 역할 |
|---|---|
| `monitor.js` | 1단계 — 접속 & 스크린샷 (배너 고정·이미지 로드 등 포함) |
| `compare.js` | 2단계 — 전일/금일 비교 (밀림 보정·움직임 제외) |
| `report.js` | 3단계 — 로컬 HTML 리포트 |
| `copy.js` | 4단계 — 구글 드라이브 폴더로 복사 |
| `publish.js` | 5단계 — 웹용 리포트(docs) 생성 (이미지 클릭 확대) |
| `notify.js` | 6단계 — 두레이 메신저 알림 |
| `run.bat` | 위 단계를 순서대로 실행 (매일 자동 실행 대상) |
| `setup.bat` | 최초 1회 설치 |
| `web-setup.bat` / `web-fix.bat` | GitHub 최초 연결 / 업로드 문제 해결 |
| `urls.csv` | 감시할 사이트 목록 |
| `webhook/share/link/weblink.txt` | 개인·팀별 설정값 (배포 제외) |
| `test-*.bat` | 특정 페이지를 빠르게 점검하는 테스트(선택, 없어도 됨) |

---

문제나 오류가 있으면, 검은 창(cmd)에 나온 메시지를 캡쳐해 담당자에게 문의하세요.
