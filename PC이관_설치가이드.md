# 사이트 모니터링 — 새 PC 설치 / 인수인계 가이드

안녕하세요! 이 문서는 **새로운 PC에서 새로운 담당자가** 이 자동 모니터링 툴을 이어받아
설치하고 운영하기 위한 안내서입니다. 순서대로 따라 하시면 됩니다. (개발 지식 없어도 OK)

> 이번 이관 기준:
> - **구글 드라이브**: 기존과 **동일한 계정** 그대로 사용
> - **GitHub**: **새 담당자 계정으로 새 저장소**를 만들어 웹 리포트 게시

---

## 이 툴이 하는 일 (1분 요약)

매일 아침 9시에 자동으로:
1. 등록된 LG U+ 랜딩페이지들(PC/모바일)의 화면을 캡쳐하고
2. 어제 화면과 비교해서 **바뀐 부분**을 찾아내고
3. 리포트(HTML)를 만들어 **브라우저로 열고 + 구글 드라이브에 저장하고 + 웹(GitHub Pages)에 올리고**
4. **두레이 메신저**로 결과를 알려줍니다.
5. 토/일 주말에는 실행하지 않습니다.

---

## 먼저 챙겨야 할 것

설치 전에 아래를 미리 확보해 두세요.

| 항목 | 무엇인지 | 준비 방법 |
|---|---|---|
| ① `site-monitor` 폴더 전체 | 프로그램 파일 묶음 | 기존 PC에서 복사 |
| ② 설정 파일 4개 | 두레이/드라이브/웹 주소 정보 | 기존 PC에서 복사 (3단계) |
| ③ 구글 드라이브 계정 | 리포트를 올리는 공유 드라이브 접근 계정 | **기존과 동일 계정** 로그인 정보 |
| ④ 새 GitHub 계정 | 웹 리포트를 게시할 새 계정 | 새로 가입 (https://github.com) |

---

## 0단계. Node.js 설치 (최초 1회)

1. https://nodejs.org 접속 → 초록색 **LTS** 버튼 클릭해 다운로드
2. 받은 파일 실행 → 전부 기본값으로 **Next → Install**
3. 확인: `Win + R` → `cmd` 입력 → 엔터 → 검은 창에 `node -v` 입력 → 엔터
   → `v22.x.x` 같은 버전이 나오면 성공

## 0-1단계. Git 설치 (웹 게시용, 최초 1회)

1. https://git-scm.com/download/win 접속 → 자동 다운로드
2. 실행 → 전부 기본값 **Next**로 설치

---

## 1단계. 폴더 옮기기

1. 기존 PC의 **`C:\site-monitor`** 폴더를 통째로 복사해 새 PC의 **`C:\site-monitor`** 로 옮깁니다.
   - USB, 사내 공유폴더, 압축(zip) 등 편한 방법 아무거나 OK
   - 용량을 줄이려면 `node_modules`, `screenshots`, `diffs`, `reports` 폴더는 빼도 됩니다
     (새 PC에서 다시 만들어짐). **단, 아래 파일들은 반드시 포함하세요.**
2. 경로는 꼭 **`C:\site-monitor`** 로. (한글·공백 없는 경로 권장)

폴더 안에 최소한 이 파일들이 있어야 합니다:
```
monitor.js  compare.js  report.js  publish.js  notify.js  copy.js
package.json  urls.csv  setup.bat  run.bat  web-setup.bat
```

---

## 2단계. 프로그램 설치 (1회만)

`setup.bat` 를 더블클릭 → 검은 창에서 자동 설치가 진행됩니다 (몇 분 소요).
`Setup complete!` 가 나오면 아무 키나 눌러 닫으세요.

> "Windows의 PC 보호" 경고가 뜨면 → **추가 정보 → 실행** 클릭

---

## 3단계. ⭐가장 중요⭐ 설정 파일 4개 챙기기

이 4개 파일은 보안상 숨겨져 있어 복사에서 누락되기 쉽습니다.
**기존 PC의 `C:\site-monitor` 에서 아래 4개 파일을 그대로 복사해 새 PC의 같은 폴더에 넣으세요.**

| 파일 이름 | 내용 | 값 |
|---|---|---|
| `webhook.txt` | 두레이 알림을 보낼 채팅방 주소 | (기존 값 그대로) |
| `share.txt` | 리포트를 저장할 구글 드라이브 폴더 경로 | `G:\내 드라이브\유플러스 랜딩페이지 모니터링` |
| `link.txt` | 팀에게 공유하는 드라이브 폴더 링크 | `https://drive.google.com/drive/folders/16Arluo0...` |
| `weblink.txt` | 웹 리포트 주소 | **6단계에서 새 주소로 교체 예정** |

> 기존 PC에서 파일이 안 보이면: 탐색기 상단 **보기 → 표시 → 숨긴 항목** 을 켜 보세요.
> `webhook.txt`, `share.txt`, `link.txt` 는 그대로 쓰고, `weblink.txt` 만 6단계에서 새 주소로 바꿉니다.

---

## 4단계. 구글 드라이브 for Desktop 설치 & 로그인 (동일 계정)

1. https://www.google.com/drive/download 에서 **Google Drive for Desktop** 설치
2. **기존과 동일한 구글 계정**으로 로그인 ← 이게 핵심입니다
3. 로그인 후 탐색기에 **`G:`** 드라이브가 생겼는지 확인
4. `G:\내 드라이브\유플러스 랜딩페이지 모니터링` 폴더가 보이면 완료
   - 동일 계정이므로 기존 경로가 그대로 보입니다. `share.txt`·`link.txt` 는 수정할 필요 없습니다.

---

## 5단계. 테스트 실행

`run.bat` 더블클릭 → 캡쳐 → 비교 → 리포트 생성 후 브라우저로 리포트가 자동으로 열립니다.

- **첫 실행**은 비교할 "어제"가 없어서 전부 **"첫 실행"** 으로 표시됩니다. 정상입니다.
- `screenshots\오늘날짜` 폴더에 이미지가 제대로(잘리지 않고) 찍혔는지 꼭 확인하세요.
- 한 번 더 `run.bat` 을 돌리면 "변경 있음/없음" 판정이 나오는 걸 볼 수 있습니다.
- 두레이 채팅방에 알림이 왔는지도 확인하세요.

> 웹 게시(6단계)를 아직 안 했다면 이 단계에서 "GitHub 업로드 실패" 메시지가 떠도 괜찮습니다.
> 캡쳐/비교/리포트/드라이브 저장/두레이 알림은 정상 동작합니다.

---

## 6단계. 웹 리포트 — 새 GitHub 계정으로 새 저장소 만들기 (최초 1회)

**(1) 새 저장소 만들기**
1. 새 GitHub 계정으로 로그인 → 우측 상단 **+ → New repository**
2. Repository name 에 `site-monitor` 입력 → **Public** 선택 → **Create repository**
   (Add README 등 체크는 하지 마세요, 빈 저장소여야 합니다)

**(2) 프로그램에 새 저장소 주소 연결 — 클로드(Claude)에게 맡기세요 (권장)**

직접 파일을 고칠 필요 없이, **이 가이드 파일을 클로드에 올린 뒤** 아래 문구를
그대로 복사해 붙여넣으세요. 빈칸에 **본인 GitHub 아이디**만 넣으면 됩니다.

```
site-monitor 새 PC 이관 중이야. 내 새 GitHub 아이디는 [여기에 아이디 입력] 이야.
web-setup.bat 의 저장소 주소를 내 계정(site-monitor 저장소)으로 바꿔주고,
weblink.txt 도 https://[내아이디].github.io/site-monitor/ 로 맞춰줘.
```

그러면 클로드가 `web-setup.bat` 과 `weblink.txt` 를 알아서 수정해 줍니다.

<details>
<summary>직접 고치고 싶다면 (수동 방법)</summary>

`web-setup.bat` 를 **메모장**으로 열어, 아래 줄의 주소를 새 저장소 주소로 바꿔 저장합니다.
```
git remote add origin https://github.com/nhnyehui/site-monitor.git
        ↓ 바꾸기 (nhnyehui → 새 계정 아이디)
git remote add origin https://github.com/새계정아이디/site-monitor.git
```
이어서 `weblink.txt` 도 `https://새계정아이디.github.io/site-monitor/` 로 바꿔 저장합니다.
</details>

**(3) 실행 & 로그인**
1. `web-setup.bat` 더블클릭
2. **GitHub 로그인 창**이 뜨면 → **새 계정**으로 로그인
3. 완료 메시지가 나오면 됩니다.

**(4) GitHub Pages 켜기**
1. GitHub에서 방금 만든 저장소 → **Settings → Pages**
2. Source: **Deploy from a branch**, Branch: **main**, 폴더: **/docs** → **Save**
3. 1~2분 뒤 `https://새계정아이디.github.io/site-monitor/` 에서 리포트가 열립니다.

**(5) 웹 주소 갱신**
- 위 새 주소(`https://새계정아이디.github.io/site-monitor/`)를 `weblink.txt` 에 붙여넣고 저장.
  (두레이 알림에 이 주소가 링크로 나갑니다)

---

## 7단계. 매일 아침 9시 자동 실행 등록

1. `Win` 키 → **작업 스케줄러** 검색 후 실행
2. 오른쪽 **기본 작업 만들기** 클릭
3. 이름: `사이트 모니터링` → 다음
4. 트리거: **매일** → 다음 → 시작 시간 **오전 9:00** → 다음
5. 동작: **프로그램 시작** → 다음 → **찾아보기**로 `C:\site-monitor\run.bat` 선택 → 다음 → 마침
6. (권장) 목록에서 방금 만든 작업 **더블클릭** → **설정** 탭 →
   **"예약된 시작을 놓친 경우 가능한 대로 빨리 작업 시작"** 체크 → 확인
   → 9시에 PC가 꺼져 있었어도 켜는 순간 바로 실행됩니다.

---

## 8단계. ⚠️기존 PC 정리 (중복 실행 방지)

새 PC가 잘 돌기 시작하면, **기존 PC의 자동 실행을 꺼야** 두레이 알림이 두 번 오지 않습니다.

- 기존 PC → 작업 스케줄러 → `사이트 모니터링` 작업 우클릭 → **사용 안 함(Disable)** 또는 **삭제**

---

## 평소 사용법

- **모니터링 페이지 추가/삭제**: `urls.csv` 를 **메모장**으로 열어 한 줄에 사이트 하나씩 수정 → 저장.
  - 칸 순서: `사이트명,URL,중요도,확인영역,무시영역,다음버튼,탭버튼,모바일`
  - 모바일 행은 URL을 `m.lguplus.com` 으로, 맨 끝 칸에 `Y` 입력
  - Excel로 편집하면 저장 시 파일 형식을 **"CSV UTF-8"** 로 선택해야 한글이 안 깨집니다
- **리포트 위치**: `C:\site-monitor\reports\index.html` (로컬), 구글 드라이브 폴더, 웹 주소(weblink.txt)
- **수동 실행**: `run.bat` 더블클릭

---

## 문제 해결

- **run.bat 창이 바로 꺼져요** → `Win+R` → `cmd` → `cd C:\site-monitor` 엔터 → `run.bat` 엔터 →
  나오는 오류 메시지를 확인(스크린샷 공유하면 확인 가능)
- **한글이 깨져요** → `urls.csv` 를 메모장에서 다시 열어 **다른 이름으로 저장 → 인코딩: UTF-8** 로 저장
- **드라이브 저장 실패** → 4단계 확인(동일 계정 로그인 / `G:` 드라이브와 대상 폴더가 보이는지)
- **웹 게시 실패** → 6단계 (2)(3)(4) 를 다시 확인. 새 저장소 주소·계정 로그인·Pages 설정이 맞아야 합니다.
- **웹 주소로 들어가면 404** → Pages 설정 후 1~2분 기다리기 / Branch가 `main`, 폴더가 `/docs` 인지 확인
- **어떤 사이트가 보안(봇 차단)에 걸려요** → 리포트에 그렇게 표시됩니다. 해당 사이트만 알려주세요.
- **롤링 배너가 매번 "변경"으로 잡혀요** → 그 페이지의 배너 CSS selector를 알려주면 1페이지 고정 처리해 드립니다.

---

궁금한 점이나 막히는 부분은 언제든 물어보세요. 특히 6단계 GitHub 연결이 어려우면
**새 GitHub 아이디만 알려주시면** `web-setup.bat` 을 대신 맞춰 드리겠습니다.
